---
import FAQPage from '../../components/pages/FAQPage.astro';
import { LANGUAGES } from '../../translations';

export function getStaticPaths() {
  return LANGUAGES.filter(l => l !== 'en').map(lang => ({ params: { lang } }));
}

const { lang } = Astro.params;
---
<FAQPage lang={lang} />
